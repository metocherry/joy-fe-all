import { css } from '@emotion/react';

import type { Spacing } from '../types';

export const PADDING: Spacing = {
  all: () => {
    return css``;
  },

  horizontal: () => {
    return css``;
  },

  vertical: () => {
    return css``;
  },

  only: () => {
    return css``;
  },
};
